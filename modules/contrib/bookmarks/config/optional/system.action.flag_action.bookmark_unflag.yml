id: flag_action.bookmark_unflag
label: 'Remove from favorites'
type: path_alias
plugin: 'flag_action:bookmark_unflag'
configuration:
  flag_id: bookmark
  flag_action: unflag
dependencies:
  config:
    - flag.flag.bookmark
  module:
    - flag
